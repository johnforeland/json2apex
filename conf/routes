# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                                       controllers.Application.index(json: Option[String] = None, className: Option[String] = None)
#* GET		/makeApex								controllers.Application.makeApex
#* POST 	/makeApex								controllers.Application.makeApex

# Catch all
#*       /{controller}/{action}                  {controller}.{action}
